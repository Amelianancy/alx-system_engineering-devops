#!/usr/bin/python3
""" Module for a function that queries the reddit api parses the title of all
hot articles and prints a sorted count of given keywords(case-insensitive),
"""
import requests


def count_words(subreddit, word_list):
    kl
